spring:
  application:
    name: '@project.name@'
  profiles:
    active: "dev"
  main:
    banner-mode: "off"
info:
  build:
    artifact: '@project.artifactId@'
    name: '@project.name@'
    description: '@project.description@'
    version: '@project.version@'